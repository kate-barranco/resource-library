{
  "name": "conscious-futures-resource-library",
  "version": "1.0.0",
  "private": true,
  "description": "Data pipeline for the Conscious Futures resource library (static site, no backend).",
  "type": "module",
  "scripts": {
    "extract": "python3 scripts/extract.py",
    "dedup": "python3 scripts/dedup.py",
    "merge-blurbs": "python3 scripts/merge_blurbs.py",
    "embed": "node scripts/precompute_embeddings.mjs",
    "sync-site-data": "python3 scripts/sync_site_data.py",
    "rebuild-data": "npm run extract && npm run dedup && npm run merge-blurbs && npm run embed && npm run sync-site-data"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
